public interface LispList{ 
  //Must have: 
  //1.Declare and initialize NIL
  //2.Declare methods empty, head, tail, cons, length, merge, contains
  boolean empty();
  Object head();
  LispList tail();
}